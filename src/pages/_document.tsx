import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html className="h-screen bg-primary">
      <Head />
      <body className="h-screen">
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
